# Copy this file to ".env" and fill in your key. ".env" is git-ignored.

# Required: your Fireworks API key (any OpenAI-compatible key works).
LLM_API_KEY=fw_...

# OpenAI-compatible endpoint. Default is Fireworks.
LLM_BASE_URL=https://api.fireworks.ai/inference/v1

# Models. Default to Fireworks gpt-oss-120b for all stages; override per stage.
# Fireworks hosts many models — swap these for a larger/stronger one for generation if you like.
MATCH_MODEL=accounts/fireworks/models/gpt-oss-120b
GENERATE_MODEL=accounts/fireworks/models/gpt-oss-120b
IMPORT_MODEL=accounts/fireworks/models/gpt-oss-120b

# Optional: max output tokens per call (generation can be large).
MAX_TOKENS=8000

# Optional: pricing used only for the cost estimate printed after each run ($ per 1M tokens).
PRICE_INPUT_PER_M=0.15
PRICE_OUTPUT_PER_M=0.60

# Optional: scoring weights (normalized automatically). Industry is the rule of thumb.
WEIGHT_INDUSTRY=0.5
WEIGHT_SKILL=0.3
WEIGHT_LOCATION=0.2

# Web backend: shared access token bidders must enter to use the platform.
# Leave blank to disable auth (local dev only). Set a long random value in production.
ACCESS_TOKEN=

# Optional: S3-compatible object storage (e.g. Supabase Storage) so generated PDFs and
# the application history survive Render's ephemeral disk. Leave blank for local-only.
# When all four of ENDPOINT/BUCKET/ACCESS_KEY_ID/SECRET are set, storage turns on.
# Supabase: Dashboard -> Project Settings -> Storage -> S3 connection (endpoint + region),
# and create S3 access keys there. Endpoint looks like the URL below.
S3_ENDPOINT_URL=https://<project-ref>.supabase.co/storage/v1/s3
S3_REGION=us-east-1
S3_BUCKET=resumes
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
# Optional key prefix inside the bucket (e.g. "prod"). Leave blank for the bucket root.
S3_PREFIX=

# Optional (alternative to S3): persist ONLY the application log to a private GitHub gist.
# Survives Render's ephemeral disk but does NOT persist the generated PDFs. When both
# GIST_TOKEN and GIST_ID are set, the gist takes precedence over S3 for the log.
# Token: a GitHub PAT with the "gist" scope. Create an empty private gist for the id.
GIST_TOKEN=
GIST_ID=
# Optional: filename inside the gist (default applications.csv).
GIST_FILENAME=applications.csv
